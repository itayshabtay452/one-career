# חוזה עבודה לסוכני AI

הקובץ הזה חל על Codex, Claude Code וכל סוכן אחר שעובד במאגר.

## סדר קריאה חובה

לפני כל שינוי:

1. קרא את `README.md`.
2. קרא את `docs/STATUS.md`.
3. קרא את ה־Issue של המשימה ואת קריטריוני הקבלה.
4. קרא את `docs/PRODUCT.md` ו־`docs/ARCHITECTURE.md` רק בחלקים הרלוונטיים.
5. בדוק `docs/decisions/` להחלטות תקפות הנוגעות למשימה.

אם אין Issue מוכן או שקריטריוני הקבלה עמומים באופן מהותי, אל תתחיל מימוש. כתוב הצעת הבהרה ב־Issue.

## כללי מקור אמת

- GitHub, הקוד והמסמכים במאגר הם מקור האמת.
- מידע שנאמר רק בצ׳אט אינו החלטה מתועדת.
- שינוי מוצר מהותי מחייב ADR ועדכון המסמכים המושפעים.
- אין למחוק החלטה היסטורית; מסמנים אותה כמוחלפת ומפנים להחלטה החדשה.
- בסיום כל משימה יש לעדכן את `docs/STATUS.md` אם מצב הפרויקט השתנה.

## Git ועבודה מקבילית

- אין לדחוף ישירות ל־`main`.
- לכל Issue יש branch או worktree משלו.
- שמות ענפים: `feat/<issue>-<name>`, `fix/<issue>-<name>`, `docs/<issue>-<name>`.
- אין לשני סוכנים לערוך במקביל אותו ענף או אותו subsystem.
- עבודה לא גמורה אך בעלת ערך נדחפת כ־WIP עם עדכון ב־Issue.
- אין לבצע Force Push ל־`main`, למחוק היסטוריה או להשתמש ב־`git reset --hard`.
- ברירת המחדל למיזוג היא Squash.

## גבולות סמכות ורמות סיכון

### ירוק

תיעוד, בדיקות, תיקון פנימי קטן או refactor ללא שינוי התנהגות. ניתן למזג לאחר CI וסקירת סוכן שני.

### צהוב

שינוי UI, חוק משחק, dependency חדש, migration לא הרסני, API ציבורי, ארכיטקטורה או שירות חיצוני. נדרש אישור בעל הפרויקט לפני מיזוג.

### אדום

מחיקת נתונים, שינוי הרשאות/Auth, סודות, שינוי רישוי, עלות חדשה, Production או migration הרסני. עוצרים לפני הפעולה ודורשים אישור מפורש, גיבוי ותוכנית חזרה.

## איכות ובדיקות

- יש להריץ את כל פקודות האיכות המוגדרות ב־`package.json` לפני פתיחת PR.
- אין להחליש, לדלג או למחוק בדיקה כדי לגרום ל־CI לעבור בלי להסביר ולתקן את הסיבה.
- מנוע הקריירה חייב להיות דטרמיניסטי: אותו Seed ואותו יומן פעולות מחזירים אותה תוצאה.
- שינוי חזותי מחייב בדיקת מובייל, צילום מסך ו־Preview כאשר התשתית קיימת.
- תיקון באג מחייב בדיקת regression כאשר הדבר מעשי.

## אבטחה ונתונים

- אין להכניס סודות, tokens, סיסמאות, מידע משתמשים או נתוני ספק מורשים ל־Git, ל־Issue או לצ׳אט.
- משתני סביבה מתועדים רק בשמותיהם בתוך `.env.example`.
- אין להשתמש ב־Service Role בדפדפן.
- כל טבלה חשופה ב־Supabase חייבת RLS ובדיקות הרשאה.
- אין להשתמש בלוגואים, תמונות שחקנים, תלבושות או נכסים רשמיים ללא אישור זכויות מפורש.

## Pull Request

כל PR נכתב בעברית וכולל:

- קישור ל־Issue.
- מה השתנה ולמה.
- מה המשתמש יראה.
- בדיקות שהורצו ותוצאותיהן.
- סיכונים, מגבלות ודרך חזרה.
- צילום מסך או וידאו לשינוי חזותי.
- מסמכים ו־ADR שעודכנו.
- זיהוי הסוכן הכותב והסוכן הסוקר.

הסוכן שכתב את השינוי אינו הסוקר היחיד שלו.

## עצירות חובה

עצור ובקש החלטה כאשר:

- קיימת סתירה בין מסמכי מקור האמת.
- נדרש שינוי החורג מקריטריוני הקבלה.
- מתגלה עלות חדשה או תלות חיצונית שלא אושרה.
- פעולה עלולה למחוק נתונים או לשבור תאימות.
- לא ניתן להסביר בעברית פשוטה מה השינוי עושה ומדוע הוא בטוח.
